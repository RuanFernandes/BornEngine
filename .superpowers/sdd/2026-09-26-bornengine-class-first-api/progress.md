# SDD ledger — plan: docs/superpowers/plans/2026-09-26-bornengine-class-first-api.md

Base: `origin/main` at `542e7bbcae197d14d95cf4363dbb0d3fa5c6bbc`.
Branch: `feature/oop-api` in `/home/nullborne/Documentos/Bornengine/.worktrees/bornengine-oop-api`.

## Pre-flight interface scan

- Task 1 -> Tasks 3, 5, 6, 8: the feature branch adds scenes, `InputActionMap`, `SoundManager`, audio FFI, and function-based imports that later class facades consume. Keep the behavior and native declarations; rewrite the public ownership/import boundary in those later tasks.
- Task 2 -> Tasks 3, 4, 5, 6, 7, 8: `GameContext` provides stable context identity, readiness, resource registration, and ownership checks. Service/resource implementations consume that same contract; Task 8 assembles them into the public `Game` class.
- Task 3 -> Task 4: `Renderer` consumes the `GameContext`; resource classes delegate drawing to that renderer and reject disposed/foreign resources.
- Task 4 -> Tasks 5, 6, 7, 9, 10: resource identity/disposal is used by audio adapters, game adapters, remaining context-bound services, docs, and the multiplayer example.
- Tasks 5 and 6 -> Task 8: the concrete `Game` facade constructs audio and scene/physics services after those classes exist.
- Task 8 -> Tasks 9 and 10: docs and the multiplayer consumer target the final class-only root/subpath exports and `0.6.0` package surface.

## Rulings

- Ruling: keep the feature branch's scene/input/audio behavior while replacing its public function dependencies — the approved spec makes the feature behavior part of the new API but rejects a function-based public surface — cost if wrong: a behavior that the user expected from the feature branch could be dropped.
- Ruling: split internal `GameContext`/`Window` from the concrete `Game` assembly, which is produced in Task 8 after service classes exist — this resolves the Task 2/3/5/6 construction cycle without changing the public API in the spec — cost if wrong: internal context types may need another refactor.
- Ruling: no tests are added or run; the higher-priority task instruction prohibits both unless the user requests them — static source/export review will be recorded instead — cost if wrong: behavior regressions may escape without automated evidence.
- Ruling: the demo's engine pin is only changed to a real immutable revision available from the configured remote — the approved spec prohibits guessed/unpublished SHAs and local paths — cost if wrong: the source migration may be ready while the dependency pin remains blocked on publishing the engine revision.

## Task status

- Task 1: complete
- Ruling: docs for this skill package reference `task-start`/`task-done`, but the installed subagent scripts provide only `task-brief`, `sdd-workspace`, and `review-package`; use `task-brief` plus direct ledger updates — no completion script exists in this installation — cost if wrong: the automated completion gate is unavailable.
- Task 1: Ruling: selected the `origin/main` versions of docs coverage and its existing coverage checks, retaining Colyseus coverage absent from the older feature-branch snapshots — matches current engine main while preserving feature assertions — cost if wrong: a small feature-branch-specific coverage expectation could be lost.
- Task 1: complete (commit recorded; merge conflicts resolved in docs coverage; diff inspected; original engine checkout, dirty MeuGame, and original multiplayer checkout unchanged; tests not run per instruction).
- Task 2: Ruling: native window creation returns `void` through the existing ABI, so `Window` marks the context ready only after that call returns and can report validation/attach failures; a native panic remains process-level unless the ABI changes — preserves the approved ABI boundary — cost if wrong: startup failures that abort inside native code cannot be inspected in TypeScript.
- Task 2: Ruling: embedded mode reserves the single runtime context until `attachNativeSurface` succeeds; `close()` remains callable once after an OS close request so native cleanup still runs — preserves host-driven rendering and idempotent shutdown — cost if wrong: a host that expects repeated low-level close calls will see one call, by design.
- Task 2: complete (commit recorded; core FFI moved behind `src/core/internal.ts`; service imports bypass the public barrel; context identity/readiness/resource disposal and window transitions reviewed by tracing source; tests not run per instruction).
- Task 3: Ruling: `InputSystem.update()` advances all maps created by that instance and each map reads only from its owning system — gives `Game.run()` one place to advance input state and avoids module-global polling — cost if wrong: consumers that manually update maps as well as rely on the game loop may advance twice until migrated.
- Task 3: complete (commit recorded; class-only `core` and `input` barrels, context-bound renderer/input services, guarded operations, camera pass balancing, and action-map ownership reviewed from source; resource draw overloads continue in Task 4; tests not run per instruction).
- Task 4: Ruling: `ImageData`, `Material`, and `Animation` expose idempotent TypeScript disposal but the existing native ABI has no per-resource release operations for those handles; `Game` teardown remains the native lifetime boundary — keeps the ABI stable — cost if wrong: those allocations can remain resident until native runtime teardown.
- Task 4: complete (commit recorded; resource loaders/handles are behind private operation modules; Texture/RenderTexture/Font/Model/Mesh/Material/Animation/SceneNode use private handles and context ownership; renderer rejects foreign resources; game scene-node adapter now stores a SceneNode class; handle-zero and repeated-disposal paths inspected; tests not run per instruction).
- Task 5: Ruling: `AudioSystem` owns the shared audio device and all Sound/Music resources; SoundManager is registered with that service and Game shutdown disposes manager voices/resources before closing the device — preserves shared-device lifetime and prevents per-manager device shutdown — cost if wrong: callers must use the owning Game's audio service for all audio.
- Task 5: Ruling: staged audio loads are represented by `StagedSound`/`StagedMusic` instances, and their native staging allocation cannot be cancelled because the ABI has no cancellation symbol — avoids public numeric staging handles — cost if wrong: abandoned staged allocations remain until audio-device shutdown.
- Task 5: complete (commit recorded; audio operation functions moved behind internal adapter; context-owned Sound/Music, staged ticket classes, AudioVoice interface, AudioSystem, manager and game-component adapters reviewed for handle-zero and repeated disposal; tests not run per instruction).
- Task 6: Ruling: scene and physics synchronization use object identity and one `PhysicsWorld.step()` boundary; static/kinematic bodies copy game transforms before the native step and dynamic bodies copy physics transforms back after it — preserves caller-controlled stepping while removing public body/world handles — cost if wrong: applications that manually synchronize and also configure `sceneManager` will apply transforms twice.
- Task 6: Ruling: world JSON stays an interoperable `WorldDocument` DTO, while `WorldData` owns file load/validate/save operations and `WorldInstance` adopts every loader-created node into context-owned `SceneNode` objects — preserves the file format while making runtime ownership explicit — cost if wrong: callers relying on the old `WorldData` interface name must migrate to `WorldDocument` and the class wrapper.
- Task 6: complete (class-bound scenes reject foreign-context scenes/resources; PhysicsWorld, collider/body/joint/character/soft-body/vehicle classes hold native IDs in a private registry; body query/contact results map to object references; physics component synchronizes around `PhysicsWorld.step()`; world load/save and prefab library are instance APIs; loader result nodes and parent links are adopted by `WorldInstance`; tests not run per instruction).
- Task 7: Ruling: game-owned services register against GameContext identity; Colyseus uses one per-context frame service so event polling does not depend on a module-global client list — maintains isolation between games — cost if wrong: Game.run must invoke the context frame-service hook for networking callbacks to advance.
- Task 7: Ruling: particle pools and the decal ring clear and invalidate their handles on disposal, but the current native ABI has no pool/ring release operations — avoids exposing IDs and prevents further API use — cost if wrong: native allocations remain until runtime shutdown.
- Task 7: Ruling: obsolete model-mesh raycast helpers and mesh-data registration were removed because no engine call site populated the registry and Model exposes no mesh CPU data — public collision now covers implemented primitives — cost if wrong: callers of the old mesh raycast need a future model-data-backed API.
- Task 7: complete (math/value classes, Collision, private shape drawing, game-owned touch/UI/VFX services, and per-context Colyseus frame service are implemented; Game.run now advances Colyseus once per frame; public barrels contain no free-function operation exports; no tests, builds, or typechecks run per instruction).

- Task 8: Ruling: reject embedded-surface attachment unless its GameContext is the active runtime owner and Game options are valid — a failed second Game must not be able to claim process-global native state — cost if wrong: an embedded Game cannot attach until the prior Game is disposed.
- Task 8: Ruling: defer web loop completion requested inside a frame to a Promise microtask — lets the web glue finish `endDrawing()` before `onStop` and resource disposal — cost if wrong: web shutdown callbacks run one microtask after the stop request.
- Task 8: Ruling: make Colyseus construction failures observable through `error`/`isLoaded` while async joins reject — matches the resource failure contract and avoids constructor exceptions — cost if wrong: callers must inspect `isLoaded` or handle join rejection.
- Task 8: complete (root and subpath barrels are explicit class/type/constant exports; `Game` owns frame pumping and orderly disposal; service/resource constructors use `Game`; package version is 0.6.0; existing game-runtime fixtures were migrated; source export/constructor searches and `git diff --check` reviewed; no test, build, or typecheck run per instruction).
